#include<iostream> 
#include<string>

using namespace std;


int main() {
	string input2;
	string inwentory[5];
	for (int i = 0;i < 5;i++)
		inventory[i] = "";
	cout << "Would you like to grab the sword??" << endl;
	if (input2.compare("yes") == 0)
		inventory[1] = "sowrd";
	cout << endl << endl;
	for (int i = 0;i < 6;i++)
		inventory[i] = "";
}